import React from 'react';
import classes from './Header.css';

const header = () => (
  <div className={classes.head}>
    <i className="fas fa-pencil-alt" style={{ marginTop: '5px', paddingRight: '10px' }} />READABLE
  </div>
);
export default header;
